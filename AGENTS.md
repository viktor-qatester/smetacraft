# ПРАВИЛА РАЗРАБОТКИ ([AGENTS.md](http://AGENTS.md))

1. ФОРМАТ ПРОЕКТА:

- Текущая фаза: автономный HTML-интерфейс в `index.html` (Vanilla JS, CSS внутри, без npm/Node-сборщиков).

- Архитектурный принцип: модульность внутри одного файла. Все математические расчеты выносятся в чистые функции-хелперы (детерминированная логика, 0 вызовов LLM).

2. ПЕРВАЯ ПОСТАВКА (Блок "Плитный фундамент"):

- Довести расчет монолитной плиты до полноценной ведомости закупки:

  * Бетон (кубы + запас 5% + марка)

  * Арматурный каркас (2 сетки, нахлест 40d, шаг ячейки, погонные метры, прутки 11.7 м, вес в кг, вязальная проволока)

  * Опалубка (доска борта в м³, брус упоров)

  * Подушка и гидроизоляция (песок, щебень с трамбованием, рулоны с нахлестом 15%)

3. ИНТЕРФЕЙС И ЗАЩИТА ОТ AI-СЛОПА:

- Двухпанельный лейаут (на экранах от 900px):

  * Слева: карточки параметров активного блока (Плита). Сверху — табы/переключатели будущих блоков (Лента, Стены, Отделка — пока в статусе "Скоро").

  * Справа (или снизу): единая "живая" спецификация закупки материалов с итоговой суммой.

- Строгий утилитарный UI: системные шрифты, четкие бордеры, отсутствие декоративных теней и градиентов.