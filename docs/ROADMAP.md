# SmetaCraft: дорожная карта Phase 1–9

Источник последовательности — `SMETACRAFT_PROJECT_CONTEXT.md` от 2026-09-13, ранее сохранённый вне репозитория. Этот файл фиксирует исходный план в самом репозитории. Статусы ниже сверены с `master` на 2026-09-18; план будущих фаз не означает разрешения начать их реализацию.

| Фаза | Цель из исходной карты | Проверенный статус |
| --- | --- | --- |
| Phase 0 | Baseline и архитектурные правила | Архитектурный аудит выполнен до Phase 1. |
| Phase 1 | Независимая golden regression safety net | Принята; основной commit `a6a8664`. |
| Phase 2 | Безопасная граница JSON и устранение небезопасного HTML | Принята; основной commit `6e2017b`. |
| Phase 3 | `NormalizedProjectModel` и адаптер старого JSON | Phase 3A (`51e0b9b`) и 3B (`598688f`) приняты и отправлены; Phase 3C реализована локально и ожидает приёмки. |
| Phase 4 | Постепенное выделение `Calculation Core` | Не начиналась; следует за принятой Phase 3. |
| Phase 5 | Frontend modules без смены дизайна | Не начиналась. |
| Phase 6 | Минимальный backend vertical slice | Не начиналась. |
| Phase 7 | Перенос проектов без автоматического удаления `localStorage` | Не начиналась. |
| Phase 8 | Закрытое файловое хранилище и импорт по одному доказанному формату | Не начиналась. |
| Phase 9 | Entitlements, usage, audit, backups и commercial readiness | Не начиналась. |

Phase 10 — AI Gateway и maintenance-agent при наличии бюджета и необходимости — обозначена в исходной карте как последующее возможное направление, за пределами девяти основных фаз.

## Точка перехода после Phase 2

В `master` подтверждены golden- и security-наборы, безопасный адаптер JSON v1, проектные скиллы, Phase 3A (`51e0b9b`) и 3B (`598688f`). Беседа «Планирование Phase 3» была планом, а не реализацией; переход к коду состоялся позднее. Phase 3C локально завершает путь ручного ввода и эквивалентность DOM/JSON. Phase 4 возможна только после приёмки всей Phase 3 и отдельного решения об архитектурном правиле `index.html`.

## Правила работы по карте

- Перед каждой фазой сверять этот план с актуальными `AGENTS.md`, `PRODUCT_SPEC.md`, кодом, тестами, Git и решением владельца. Код и принятый scope имеют приоритет над устаревшим описанием.
- Для каждой фазы отдельно утверждать ограниченный scope, критерии выхода, регрессионные проверки и способ отката. Не начинать следующую фазу автоматически.
- Сохранять контракт: одинаковые вход и цены дают те же строки, количества, коэффициенты и суммы, если владелец явно не утвердил изменение поведения. Не переписывать `tests/golden.json` ради прохождения тестов.
- Phase 4 и Phase 5 предполагают постепенное разделение приложения, тогда как действующий `AGENTS.md` требует монолитный `index.html`. До реализации этих фаз отдельно согласовать изменение архитектурного правила; сама карта его не отменяет.
- Commit, push, PR и deploy выполнять только при отдельном разрешении для соответствующей фазы.
